import 'package:flutter/material.dart';

class ProfileWithFacebook extends StatelessWidget {
  const ProfileWithFacebook({Key? key,required this.userEmail}) : super(key: key);
  final String userEmail;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
